research/intraday/bars/
dashboard/dashboard.html

# Python bytecode (never belongs in the repo; swept in accidentally by a git add -A)
__pycache__/
*.pyc

# Synced Codex-review data: refreshed 6x/trading day from the droplet, transient. A reviewer
# dedups via reports/latest/manifest.json (sha16 per file), so this need not be version-controlled.
reports/latest/
# Codex's own review outputs are kept locally, not committed by default (uncomment to track):
# reports/reviews/

# ── Large research data: stripped from history 2026-07-21 (exceeded GitHub's 100 MB limit) ─────
# These are regenerable caches/flow dumps, not source. Kept on disk locally, never in git.
simulations/api_cache_overlay_batch.db
simulations/api_cache_overlay_batch.db-shm
simulations/api_cache_overlay_batch.db-wal
simulations/api_cache_overlay.db*
simulations/whalestream_flow_*.csv
simulations/whalestream_flow_*.csv.gz
# Any large data that would trip GitHub's limits again:
*.parquet
research/**/*.parquet

# ── Secrets: NEVER commit (defense-in-depth before pushing to the private remote) ──────────────
.env
*.env
.env.*
prod_creds*
*.pem
*.key
id_ed25519*
id_rsa*
**/credentials.json
*token.txt
*secret*.txt

# ── Transient analysis outputs, dumps & scratch (item 7, 2026-07-21): kept local, never in git /
#    never swept into a Codex patch branch. Source .py under research/ & simulations/ is NOT ignored
#    here — those stay classifiable/committable; only their DATA outputs and scratch are ignored. ──
droplet_data/
scratch_*.py
cboe_tmp.html
*.zip
*.docx
# Large / regenerable research + flow data (keep under GitHub's limits and out of review diffs)
research/**/*.csv.gz
research/**/*.log
research/convexity/cards/
research/convexity/events.csv
research/convexity/outcomes.json
research/skew/
research/llm_gate_backtest/
research/slice_table_for_report.csv
research/tradier_truth_outcomes.csv
# simulations: DATA outputs only (the *.py backtest sources remain trackable)
simulations/*.csv
simulations/*_results.json
simulations/flow_degross_events.json
simulations/flow_degross_run.log
simulations/_barth_check.txt
